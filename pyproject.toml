[build-system]
requires = ["setuptools>=69"]
build-backend = "setuptools.build_meta"

[project]
name = "text-of-joker-simulator-reborn"
version = "0.1.0"
description = "Reborn Text of Joker simulator prototype"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["src"]

